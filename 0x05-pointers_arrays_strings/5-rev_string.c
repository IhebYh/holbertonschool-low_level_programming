#include "main.h"

/**
 * rev_string - entry point
 * @s :char
 * Return:void
 */
void rev_string(char *s)
{
int i, n;
char *rev;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
n = i - 1;
for (i = n; i >= 0 ; i--)
{
rev += s[i];
}
}
